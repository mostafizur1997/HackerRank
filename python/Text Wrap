import textwrap

def wrap(string, max_width):
    a=textwrap.TextWrapper(width=max_width)
    wrapList=a.fill(text=string)
    return (wrapList)
    

if __name__ == '__main__':
    string, max_width = input(), int(input())
    result = wrap(string, max_width)
    print(result)
